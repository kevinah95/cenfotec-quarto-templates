branches:
  - main
  - name: alpha
    prerelease: true

tagFormat: "assigment-v${version}"

plugins:
  - - "@semantic-release/commit-analyzer"
    - releaseRules:
        # Only release when assigment scope is used
        - type: feat
          scope: assigment
          release: minor
        - type: fix
          scope: assigment
          release: patch
        - type: perf
          scope: assigment
          release: patch
        - breaking: true
          scope: assigment
          release: major
        # Ignore changes to other scopes
        - scope: "!assigment"
          release: false
      parserOpts:
        noteKeywords:
          - BREAKING CHANGE
          - BREAKING CHANGES

  - - "@semantic-release/release-notes-generator"
    - preset: angular
      writerOpts:
        commitsSort:
          - subject
          - scope
      presetConfig:
        types:
          - type: feat
            section: Features
          - type: fix
            section: Bug Fixes
          - type: perf
            section: Performance Improvements
          - type: docs
            section: Documentation
          - type: refactor
            section: Code Refactoring

  - - "@semantic-release/exec"
    - prepareCmd: "node scripts/sync-template-version.mjs assigment ${nextRelease.version}"

  - - "@semantic-release/changelog"
    - changelogFile: "assigment/CHANGELOG.md"
      changelogTitle: "# Changelog - Assigment Template"

  - - "@semantic-release/git"
    - assets:
        - "assigment/CHANGELOG.md"
        - "assigment/_extensions/assigment/_extension.yml"
      message: "chore(release): assigment v${nextRelease.version}\n\n${nextRelease.notes}"

  - - "@semantic-release/github"
    - successComment: false
      releasedLabels: false
